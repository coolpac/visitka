# ⚡ Быстрый старт деплоя

## 🚀 За 5 минут на продакшен

### Шаг 1: Подготовка проекта

```bash
# Убедитесь, что .env файл настроен
cd bots
cp .env.example .env
nano .env  # Заполните токены
```

### Шаг 2: Настройка сервера (один раз)

```bash
# Скопируйте скрипт на сервер
scp deploy/setup-server.sh root@81.200.153.155:/tmp/

# Запустите на сервере
ssh root@81.200.153.155 "bash /tmp/setup-server.sh"
```

### Шаг 3: Деплой проекта

```bash
# С локальной машины
chmod +x deploy/deploy.sh
./deploy/deploy.sh root@81.200.153.155
```

### Шаг 4: Настройка DNS

В панели управления доменом добавьте:
- **A**: `annaivaschenko.ru` → `81.200.153.155`
- **A**: `www.annaivaschenko.ru` → `81.200.153.155`

Подождите 5-15 минут.

### Шаг 5: Получение SSL

```bash
ssh root@81.200.153.155
cd /var/www/annaivaschenko.ru/deploy
chmod +x ssl-setup.sh
# Отредактируйте email в ssl-setup.sh
./ssl-setup.sh
```

## ✅ Готово!

Откройте https://annaivaschenko.ru

## 📋 Проверка

```bash
# Статус ботов
ssh root@81.200.153.155 "systemctl status user-bot.service admin-bot.service"

# Логи
ssh root@81.200.153.155 "tail -f /var/log/annaivaschenko/user-bot.log"
```

## 🔄 Обновление проекта

После изменений:

```bash
./deploy/deploy.sh root@81.200.153.155
```

