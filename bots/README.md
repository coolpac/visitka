# Telegram Bots для мини-приложения

Два Telegram бота для работы с мини-приложением: User Bot (для пользователей) и Admin Bot (для администраторов).

## 📋 Содержание

- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Функционал](#функционал)
- [Структура проекта](#структура-проекта)

## 🚀 Установка

### 1. Установите Python 3.8+

Проверьте версию Python:
```bash
python3 --version
```

### 2. Установите зависимости

```bash
cd bots
pip install -r requirements.txt
```

Или используйте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## ⚙️ Настройка

### 1. Создайте ботов через @BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания двух ботов:
   - **User Bot** - для пользователей
   - **Admin Bot** - для администраторов
4. Сохраните полученные токены

### 2. Узнайте свой Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему любое сообщение
3. Скопируйте ваш ID (число)

### 3. Настройте админ бот для получения уведомлений

1. Запустите Admin Bot
2. Отправьте ему команду `/start`
3. Найдите бота [@userinfobot](https://t.me/userinfobot)
4. Перешлите сообщение от Admin Bot в @userinfobot
5. Скопируйте ID чата (это будет `ADMIN_BOT_CHAT_ID`)

### 4. Создайте файл `.env`

```bash
cp .env.example .env
```

**📖 Подробная инструкция**: См. [ENV_SETUP.md](ENV_SETUP.md) для пошаговой настройки.

Отредактируйте `.env` и заполните все значения:

```env
USER_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_BOT_TOKEN=9876543210:XYZwvuTSRqpoNMLkjihGFEdcba
ADMIN_BOT_CHAT_ID=123456789
ADMIN_IDS=123456789,987654321
WEB_APP_URL=https://your-domain.com
```

### 5. Настройте Web App для User Bot

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите ваш User Bot
3. Отправьте команду `/newapp`
4. Следуйте инструкциям:
   - Название: "Визитка Анны Иващенко"
   - Описание: "Мини-приложение с информацией о профессиональной деятельности"
   - Фото (опционально)
   - URL: ваш `WEB_APP_URL`
   - Короткое имя: `visitka` (или другое)

## 🏃 Запуск

### Запуск User Bot

В отдельном терминале:
```bash
cd bots
python3 user_bot.py
```

### Запуск Admin Bot

В отдельном терминале:
```bash
cd bots
python3 admin_bot.py
```

### Запуск через systemd (Linux) или PM2 (Node.js)

Для постоянной работы ботов на сервере используйте:
- **systemd** (Linux)
- **PM2** (через `pm2 start user_bot.py --interpreter python3`)
- **Docker** (создайте Dockerfile)

## 📱 Функционал

### User Bot (Бот для пользователей)

#### Команды:
- `/start` - Приветственное сообщение с кнопкой для открытия мини-приложения
- `/help` - Справка по командам

#### Функции:
- ✅ Приветственное сообщение при команде `/start`
- ✅ Кнопка Web App для открытия мини-приложения
- ✅ Сбор start параметров (например, `/start ref123`)
- ✅ Автоматическая отправка уведомлений в Admin Bot о новых пользователях
- ✅ Сохранение информации о пользователях в базе данных

### Admin Bot (Бот для администраторов)

#### Команды:
- `/start` - Начать работу с ботом
- `/stats` - Статистика пользователей
- `/broadcast` - Создать рассылку всем пользователям
- `/help` - Справка по командам
- `/cancel` - Отменить текущую операцию

#### Функции:
- ✅ Просмотр статистики пользователей
- ✅ Создание рассылки для всех пользователей User Bot
- ✅ **Поддержка изображений** в рассылке
- ✅ **Поддержка inline кнопок** со ссылками
- ✅ Предпросмотр сообщения перед отправкой
- ✅ **Красивая анимация прогресса** отправки
- ✅ Статистика рассылок (отправлено/ошибок)
- ✅ Сохранение истории рассылок

## 📁 Структура проекта

```
bots/
├── config.py          # Конфигурация ботов
├── database.py        # Работа с базой данных
├── user_bot.py       # User Bot (бот для пользователей)
├── admin_bot.py      # Admin Bot (бот для администраторов)
├── requirements.txt  # Зависимости Python
├── .env.example      # Пример файла конфигурации
├── .env              # Файл конфигурации (создайте сами)
├── database.db       # База данных SQLite (создается автоматически)
└── README.md         # Эта документация
```

## 🔧 Настройка базы данных

База данных создается автоматически при первом запуске бота. Файл `database.db` будет создан в папке `bots/`.

### Таблицы:

1. **users** - Пользователи User Bot
   - `user_id` - ID пользователя Telegram
   - `username` - Username пользователя
   - `first_name` - Имя пользователя
   - `last_name` - Фамилия пользователя
   - `start_param` - Start параметр (если был передан)
   - `registered_at` - Дата регистрации
   - `last_activity` - Последняя активность
   - `is_active` - Активен ли пользователь

2. **broadcasts** - История рассылок
   - `id` - ID рассылки
   - `admin_id` - ID администратора
   - `message_text` - Текст сообщения
   - `sent_count` - Количество отправленных сообщений
   - `failed_count` - Количество ошибок
   - `created_at` - Дата создания

## 📊 Примеры использования

### User Bot

Пользователь отправляет `/start`:
```
👋 Добро пожаловать!

Я бот-помощник Анны Алексеевны Иващенко.

Нажмите на кнопку ниже, чтобы открыть мини-приложение...

[🚀 Открыть мини-приложение]
```

### Admin Bot

Администратор отправляет `/stats`:
```
📊 Статистика пользователей:

👥 Всего пользователей: 42
✅ Активных пользователей: 38
```

Администратор отправляет `/broadcast`:
```
📢 Создание рассылки

Отправьте контент для рассылки:
• Текст сообщения
• Фото с подписью
• Только фото

[Админ отправляет фото с текстом]

Хотите добавить кнопки? Отправьте:
• да или кнопки - добавить кнопки
• нет или пропустить - продолжить без кнопок

[Админ отправляет: да]

🔘 Добавление кнопок

Отправьте кнопки в формате:
Текст кнопки 1 | https://ссылка1.com
Текст кнопки 2 | https://ссылка2.com

[Админ отправляет кнопки]

✅ Добавлено кнопок: 2
Показываю предпросмотр...

[Предпросмотр с фото и кнопками]

Отправьте да для подтверждения...

[Админ отправляет: да]

⏳ Рассылка в процессе...
━━━━━━━━━━━━━━━━━━━━
████████████████████ 100%
👥 Пользователей: 50/50
✅ Отправлено: 48
❌ Ошибок: 2

✅ Рассылка завершена!
```

**Подробное руководство**: См. [BROADCAST_GUIDE.md](BROADCAST_GUIDE.md)

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте, что токены в `.env` правильные
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Ошибка "ADMIN_BOT_CHAT_ID не установлен"

1. Убедитесь, что Admin Bot запущен и вы отправили ему `/start`
2. Найдите ID чата через @userinfobot
3. Добавьте ID в `.env`

### Ошибка "У вас нет доступа"

1. Проверьте, что ваш ID добавлен в `ADMIN_IDS` в `.env`
2. Убедитесь, что ID указан правильно (число, без пробелов)

### База данных не создается

1. Проверьте права на запись в папку `bots/`
2. Убедитесь, что путь к базе данных указан правильно

## 📝 Логирование

Боты логируют все важные события:
- Регистрация новых пользователей
- Отправка рассылок
- Ошибки при работе

Логи выводятся в консоль в формате:
```
2024-01-01 12:00:00 - user_bot - INFO - Новый пользователь зарегистрирован: 123456789
```

## 🔒 Безопасность

1. **Никогда не публикуйте файл `.env`** в репозиторий
2. Добавьте `.env` в `.gitignore`
3. Храните токены в безопасности
4. Ограничьте доступ к Admin Bot только доверенным администраторам

## 📚 Дополнительные ресурсы

- [Документация aiogram](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Web Apps](https://core.telegram.org/bots/webapps)

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи ботов
2. Правильность настройки `.env`
3. Доступность интернета
4. Статус Telegram API


