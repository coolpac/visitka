# 🔧 Настройка .env файла

Пошаговая инструкция по настройке конфигурации ботов.

## 📋 Шаг 1: Создайте файл .env

```bash
cd bots
cp .env.example .env
```

## 📋 Шаг 2: Получите токены ботов

### User Bot (бот для пользователей)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Визитка Анны Иващенко`)
4. Введите username бота (например: `annaivaschenko_bot`)
5. Скопируйте токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. Вставьте в `.env` файл в поле `USER_BOT_TOKEN`

### Admin Bot (бот для администраторов)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Admin Bot - Анна Иващенко`)
4. Введите username бота (например: `annaivaschenko_admin_bot`)
5. Скопируйте токен
6. Вставьте в `.env` файл в поле `ADMIN_BOT_TOKEN`

## 📋 Шаг 3: Узнайте ваш Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Он покажет ваш ID (число, например: `123456789`)
4. Вставьте этот ID в `.env` файл в поле `ADMIN_IDS`

Если у вас несколько администраторов, укажите ID через запятую **БЕЗ пробелов**:
```
ADMIN_IDS=123456789,987654321,111222333
```

## 📋 Шаг 4: Настройте Admin Bot Chat ID

Это ID чата, куда будут приходить уведомления о новых пользователях.

1. Запустите Admin Bot (если еще не запущен)
2. Отправьте ему команду `/start`
3. Найдите бота [@userinfobot](https://t.me/userinfobot)
4. Перешлите любое сообщение от Admin Bot в @userinfobot
5. Скопируйте **Chat ID** (это будет число, например: `123456789`)
6. Вставьте в `.env` файл в поле `ADMIN_BOT_CHAT_ID`

**Важно**: Это должен быть Chat ID, а не User ID. Если просто отправить сообщение @userinfobot, он покажет User ID. Нужно переслать сообщение от бота.

## 📋 Шаг 5: Настройте URL приложения

Для продакшена:
```
WEB_APP_URL=https://annaivaschenko.ru
```

Для разработки (локально):
```
WEB_APP_URL=http://localhost:8000
```

## ✅ Пример заполненного .env файла

```env
USER_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890
ADMIN_BOT_TOKEN=9876543210:XYZwvuTSRqpoNMLkjihGFEdcba9876543210
ADMIN_BOT_CHAT_ID=123456789
ADMIN_IDS=123456789
WEB_APP_URL=https://annaivaschenko.ru
DATABASE_FILE=bots/database.db
```

## 🔒 Безопасность

⚠️ **ВАЖНО**: 
- Никогда не публикуйте файл `.env` в git
- Не делитесь токенами с другими
- Храните `.env` файл в безопасности

Файл `.env` уже добавлен в `.gitignore`, поэтому он не попадет в репозиторий.

## 🧪 Проверка настроек

После заполнения `.env` файла, проверьте:

```bash
cd bots
python3 -c "from config import *; print('✅ Конфигурация загружена успешно')"
```

Если видите ошибку, проверьте:
1. Все ли поля заполнены
2. Правильно ли указаны токены (без лишних пробелов)
3. Правильно ли указаны ID (только числа)

## 🚀 Готово!

Теперь можно запускать ботов:

```bash
python3 user_bot.py   # В одном терминале
python3 admin_bot.py  # В другом терминале
```

