#!/bin/bash

# Скрипт для остановки ботов
# Использование: ./stop_bots.sh

echo "🛑 Остановка Telegram ботов..."

if [ -f bots.pid ]; then
    PIDS=$(cat bots.pid)
    kill $PIDS 2>/dev/null
    rm bots.pid
    echo "✅ Боты остановлены"
else
    echo "⚠️  Файл bots.pid не найден"
    echo "Попытка найти процессы вручную..."
    
    # Поиск процессов Python с ботами
    pkill -f "user_bot.py"
    pkill -f "admin_bot.py"
    
    echo "✅ Попытка остановки завершена"
fi


